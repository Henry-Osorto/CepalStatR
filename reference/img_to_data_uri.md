# Convert an image file to a data URI

Convert an image file to a data URI

## Usage

``` r
img_to_data_uri(path)
```

## Arguments

- path:

  Path to the image file.

## Value

A character string containing a data URI, or NULL if the file does not
exist.
