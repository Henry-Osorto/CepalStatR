% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyramids.R
\name{pyramids}
\alias{pyramids}
\title{Population pyramids}
\usage{
pyramids(
  country,
  years = 1:31,
  language.en = TRUE,
  color = c("#0C4A61", "#34B0AA"),
  save = FALSE,
  file = NULL,
  format = c("png", "pdf", "svg"),
  height = 5,
  width = 7,
  caption = TRUE
)
}
\arguments{
\item{country}{Character. Name of the country or region to plot.}

\item{years}{Numeric vector of positions between 1 and 31 corresponding to
5-year intervals from 1950 to 2100.}

\item{language.en}{Logical. If \code{TRUE} (default), English labels are used.
If \code{FALSE}, Spanish labels are used.}

\item{color}{Character vector of length 2 with colors for men and women.}

\item{save}{Logical. If \code{TRUE}, saves the resulting figure.}

\item{file}{Character. Output filename when \code{save = TRUE}. If \code{NULL}, a default
filename is used.}

\item{format}{Character. Output format when \code{save = TRUE}. One of \code{"png"}
(default), \code{"pdf"} or \code{"svg"}.}

\item{height}{Numeric. Height of saved figure.}

\item{width}{Numeric. Width of saved figure.}

\item{caption}{Logical. If \code{TRUE}, adds a caption with source information.}
}
\value{
Invisibly returns a grob object created by \code{gridExtra::arrangeGrob()}.
}
\description{
Creates one or multiple population pyramids for a selected country and years
using CEPALSTAT population-by-age-and-sex data.
}
\examples{
\dontrun{
# Single year
pyramids(country = "Honduras", years = 16)

# Multiple years
pyramids(country = "Honduras", years = c(1, 5, 10, 15))

# Save as PNG
pyramids(
  country = "Honduras",
  years = c(1, 5, 10, 15),
  save = TRUE
)

# Save as PDF
pyramids(
  country = "Honduras",
  years = c(1, 5, 10, 15),
  save = TRUE,
  format = "pdf",
  file = "pyramids.pdf"
)

# Save as SVG
pyramids(
  country = "Honduras",
  years = c(1, 5, 10, 15),
  save = TRUE,
  format = "svg",
  file = "pyramids.svg"
)
}
}
