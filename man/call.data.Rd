% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call.data.R
\name{call.data}
\alias{call.data}
\title{Call Data Indicators CEPALSTAT}
\usage{
call.data(id.indicator, language.en = TRUE, notes = FALSE,
                 progress = TRUE, add.indicator.name = TRUE)
}
\arguments{
\item{id.indicator}{A single numeric or character CEPALSTAT indicator ID.}

\item{language.en}{Logical. If \code{TRUE} (default), the output uses English labels.
If \code{FALSE}, Spanish labels are used when applicable.}

\item{notes}{Logical. If \code{TRUE}, footnotes are joined when available.}

\item{progress}{Logical. If \code{TRUE}, progress messages are shown.}

\item{add.indicator.name}{Logical. If \code{TRUE}, adds indicator ID and indicator name.}
}
\value{
A data frame with indicator values, dimension labels, and metadata.
When \code{add.indicator.name = TRUE}, the indicator identifier is included as
a character string.
}
\description{
Retrieves indicator data from the CEPALSTAT API in JSON format and returns
a data frame with values, dimensions, metadata, and optionally footnotes.
}
\details{
Returned objects include provenance metadata attributes that record
the retrieval timestamp, package version, indicator identifier,
language and API endpoint used for the request.
}
\examples{
\donttest{
data <- call.data(id.indicator = 4788)
data <- call.data(id.indicator = 4788, language.en = FALSE)
data <- call.data(id.indicator = 4788, notes = TRUE)
}
}
