% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranking.sdg.R
\name{ranking.sdg}
\alias{ranking.sdg}
\title{Sustainable Development Goals ranking}
\usage{
ranking.sdg(
  id.indicator,
  language.en = TRUE,
  save = FALSE,
  file = NULL,
  format = c("png", "pdf", "svg"),
  height = 5,
  width = 9,
  size.title = 10,
  title = TRUE,
  caption = TRUE,
  color = "#032B47",
  color.gc = "#36B3FF"
)
}
\arguments{
\item{id.indicator}{Numeric or character. Indicator ID.}

\item{language.en}{Logical. If \code{TRUE} (default), English labels are used.
If \code{FALSE}, Spanish labels are used.}

\item{save}{Logical. If \code{TRUE}, saves the resulting figure.}

\item{file}{Character. Output filename when \code{save = TRUE}. If \code{NULL}, a default
filename is used.}

\item{format}{Character. Output format when \code{save = TRUE}. One of \code{"png"}
(default), \code{"pdf"} or \code{"svg"}.}

\item{height}{Numeric. Height of saved figure.}

\item{width}{Numeric. Width of saved figure.}

\item{size.title}{Numeric. Title size.}

\item{title}{Logical. If \code{TRUE}, displays the chart title.}

\item{caption}{Logical. If \code{TRUE}, displays a note indicating the latest
available year by country.}

\item{color}{Character. Color for countries in the ranking.}

\item{color.gc}{Character. Color for regional aggregates if present.}
}
\value{
Invisibly returns a \code{ggplot} object.
}
\description{
Creates a ranking chart for a CEPALSTAT indicator belonging to the
Sustainable Development Goals (SDG/ODS) dimension, using the latest
available data by country.
}
\examples{
\dontrun{
ranking.sdg(id.indicator = 3682)
ranking.sdg(id.indicator = 3682, language.en = FALSE)

ranking.sdg(
  id.indicator = 3682,
  save = TRUE
)

ranking.sdg(
  id.indicator = 3682,
  save = TRUE,
  format = "pdf",
  file = "ranking_sdg.pdf"
)

ranking.sdg(
  id.indicator = 3682,
  save = TRUE,
  format = "svg",
  file = "ranking_sdg.svg"
)
}
}
